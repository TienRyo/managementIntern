### Survey Management Client

How to run:

1. Clone repository: `git clone https://github.com/sphinx-software/survey-management-client.git`
2. Create file `.env` in project
3. Copy file content `.env.example` and paste in `.env`
4. Run `npm start`
